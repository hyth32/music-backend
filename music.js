// (async () => {
//     const trackName = 'Девочка пиздец'
//     // const tracks = await search('track', trackName)

//     if (tracks.length && tracks.length > 0) {
//         console.log(`Ссылка на первый трек: ${tracks[0].external_urls.spotify}`)
//     }
// })()